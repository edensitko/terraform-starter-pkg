# ---------------------------------------------------------------------------------------------------------------------
# REMOTE BACKEND — GCS
# Generated by tf-starter
# ---------------------------------------------------------------------------------------------------------------------

terraform {
  backend "gcs" {
    ### MUST EDIT THIS ###
    bucket = "{{ project_name }}-terraform-state"
    prefix = "{{ project_name }}/terraform.tfstate"
  }
}